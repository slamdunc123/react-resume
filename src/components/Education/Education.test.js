import React from 'react';
import { render } from '@testing-library/react';
import Education from './Education';

test('renders Education component', () => {
	const result = render(<Education />);
});

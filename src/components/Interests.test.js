import React from 'react';
import { render } from '@testing-library/react';
import Interests from './Interests';

test('renders Interests component', () => {
	const result = render(<Interests />);
});
